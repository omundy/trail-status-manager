document.addEventListener('DOMContentLoaded', function () {
	var trailmanager = document.querySelector('meta[name="trail-manager"]');
	if (trailmanager) {
		console.log("trailmanager =", trailmanager.content);

		// now hide all the options

	}

}, false);
