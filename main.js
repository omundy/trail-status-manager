document.addEventListener('DOMContentLoaded', function () {
	var trailManager = document.querySelector('meta[name="trail-manager"]').content;
	if (trailManager) {
		console.log("trailManager =", trailManager);

		// now hide all the options
         
	}

}, false);
